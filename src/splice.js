//Splice (its changing the original array which can be add, remove, replace)

let arr = [12, 34, 54, 34, 87, 23, 54, 23];

// arr.splice(1, 3, 100, 101);

//Add Element
// arr.splice(1, 0, 200, 300);

// Delete Element
// arr.splice(2,1);

//Replace ELement
arr.splice(1, 1, 36);